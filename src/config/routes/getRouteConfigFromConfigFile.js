import assert from 'assert';
// import { readFileSync } from 'fs';

const getRouteConfigFromConfigFile = configFile => {
    // const routesConfig = JSON.parse(readFileSync(configFile));
    // assert(Array.isArray(routesConfig), `routes config should be Array, but got ${routesConfig}`);
    // return routesConfig;
};

export default getRouteConfigFromConfigFile;
